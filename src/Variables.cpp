#include "Variables.h"
#include <string>

std::string Channel_Name = ""; // User's channel name (initially empty)

double Money = 0; // User's current money amount
double Subs = 0; // User's total subscriptions
double Views = 0; // User's total views

double Equipment_Quality = 1.5; // User's equipment quality

int Total_Video = 0; // User's total videos
